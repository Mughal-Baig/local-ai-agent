AgentTrail 0.7.0

Use desktop/linux/agenttrail.desktop or desktop/linux/agenttrail-tray.sh. Package templates are in installers/linux for deb, rpm, and AppImage-style staging.

This repository includes launchers, update metadata, notification hooks, checksum generation, and signing command scaffolds. Native signing requires platform certificates.
